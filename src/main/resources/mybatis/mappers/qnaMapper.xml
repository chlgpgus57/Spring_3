<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="qnaMapper">
  
  <select id="qnaList" parameterType="com.hh.s3.util.Pager" resultType="com.hh.s3.model.board.QnaVO">
  	select * 
  	from (select rownum R, Q.*
  	from (select * from qna
  		<choose>
  			<when test="kind == 'kt'">
  				 where title like '%'||#{search}||'%'
  			</when>
  			<when test="kind == 'kw'">
  				 where writer like '%'||#{search}||'%'
  			</when>
  			<otherwise>
  				 where contents like '%'||#{search}||'%'		
  			</otherwise>
  		</choose>
  	order by ref desc, step asc) Q)
  	where R between #{startRow} and #{lastRow}
  </select>
  
  <select id="qnaCount" parameterType="com.hh.s3.util.Pager" resultType="java.lang.Integer" >
  	select count(num) from qna
  	<choose>
  			<when test="kind == 'kt'">
  				 where title like '%'||#{search}||'%'
  			</when>
  			<when test="kind == 'kw'">
  				 where writer like '%'||#{search}||'%'
  			</when>
  			<otherwise>
  				 where contents like '%'||#{search}||'%'		
  			</otherwise>
  		</choose>
  </select>
  
  <select id="qnaSelect" parameterType="java.lang.Integer" resultType="com.hh.s3.model.board.QnaVO">
  	select * from qna where num=#{num}
  </select>
  
  <insert id="qnaWrite" parameterType="com.hh.s3.model.board.QnaVO">
  	insert into qna values(board_seq.nextval , #{title}, #{writer},#{contents}, sysdate, 0, board_seq.currval ,0,0)
  </insert>
  
  <insert id="qnaReply" parameterType="com.hh.s3.model.board.QnaVO">
  	insert into qna values(
  		board_seq.nextval,
  		#{title},
  		#{writer},
  		#{contents},
  		sysdate,
  		0,
  		#{ref},
  		#{step},
  		#{depth}
  	)
  </insert>
  
  
  <update id="qnaReplyUpdate" parameterType="com.hh.s3.model.board.QnaVO">
  	update qna set step=step+1
  	where ref=#{ref} and step > #{step}
  </update>
  
  <delete id="qnaDelete" parameterType="com.hh.s3.model.board.QnaVO">
  	delete qna where num=#{num}
  </delete>
  
  
</mapper>